import { krakenUiWebTestCard } from './kraken-ui-web-test-card';

describe('krakenUiWebTestCard', () => {
  it('should work', () => {
    expect(krakenUiWebTestCard()).toEqual('kraken-ui-web-test-card');
  });
});
